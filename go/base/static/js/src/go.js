// go
// ==
// Base module for the client side of Go

(function(exports) {
  var urls = {
    loading: '/static/img/loading.gif'
  };

  _(exports).extend({
    urls: urls
  });
})(window.go = window.go || {});

describe("go.campaigns.setup.interactive", function() {
  var module = go.campaigns.setup.interactive;

  describe(".dummy", function() {
    // TODO remove once module is implemented
    it("should return 1", function() {
      assert.equal(module.dummy(), 1);
    });
  });
});

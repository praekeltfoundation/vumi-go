<span class='text'><%= model.text %></span>
<ol class='choices'>
<% _(model.choice_endpoints).each(function(choice) { %>
  <li data-endpoint-id="<%= choice.uuid %>"><%= choice.label %></li>
<% }); %>
</ol>

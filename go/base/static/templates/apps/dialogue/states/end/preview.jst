<p class="text"><%= model.get("text") %></p>
<p class="hint text-info">Recipients can't answer</p>
<p class="hint text-danger">Session ends</p>

<div class="char-count form-group text-right <%= state.tooManyChars() %>">
  <%= state.calcChars() %> characters used.
</div>